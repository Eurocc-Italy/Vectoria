{
    "flag": [
        {
            "name": "--faiss-index",
            "param": {
                "type": "str",
                "required": "True",
                "help": "The path to the faiss index (in pickle format)"
            }
        },
        {
            "name": "--k",
            "param": {
                "type": "int",
                "required": "True",
                "help": "The number of retrieved chunks"
            }
        },
        {
            "name": "--test-set-path",
            "param": {
                "type": "str",
                "required": "True",
                "help": "A yaml file defining a set of questions to evaluate the model on."
            }
        },
        {
            "name": "--generate-answers",
            "param": {
                "type": "bool",
                "required": "False",
                "default": "False",
                "help": "Generate answers for the test set."
            }
        }     
    ]
}
