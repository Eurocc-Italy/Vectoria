{
    "flag": [
        {
            "name": "--test-set-path",
            "param": {
                "type": "str",
                "required": "True",
                "help": "A yaml file defining a set of questions to evaluate the model on."
            }
        },
        {
            "name": "--output-dir",
            "param": {
                "type": "str",
                "required": "True",
                "help": "The path to the output directory"
            }
        },
        {
            "name": "--evaluation-tool",
            "param": {
                "type": "str",
                "required": "False",
                "default": "ragas",
                "help": "The evaluation tool to use",
                "choices": ["ragas"]
            }
        }
    ]
}
