{
    "flag": [
        {
            "name": "--faiss_index",
            "param": {
                "type": "str",
                "required": "True",
                "help": "The path to the faiss index (in pickle format)"
            }
        },
        {
            "name": "--k",
            "param": {
                "type": "int",
                "required": "True",
                "help": "The number of retrieved chunks"
            }
        },
        {
            "name": "--query",
            "param": {
                "type": "str",
                "required": "True",
                "help": "The desired query to prompt the RAG system"
            }
        }
    ]
}
